-

  export const NumberBoxYDF_GUI_ComponentLocalization__Russian = {
    buttons: {
      incrementing: {
        generateAccessibilityGuidance: ({ step }) => `Увеличить значение на ${ step }`
      },
      decrementing: {
        generateAccessibilityGuidance: ({ step }) => `Уменьшить значение на ${ step }`
      }
    }
  };
