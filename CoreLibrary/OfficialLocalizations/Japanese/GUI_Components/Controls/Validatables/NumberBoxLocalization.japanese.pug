-

  const NumberBoxYDF_GUI_ComponentLocalization__Japanese = {
    buttons: {
      incrementing: {
        generateAccessibilityGuidance: ({ step }) => `${ step }で増やす`
      },
      decrementing: {
        generateAccessibilityGuidance: ({ step }) => `${ step }で減らす`
      }
    }
  };
