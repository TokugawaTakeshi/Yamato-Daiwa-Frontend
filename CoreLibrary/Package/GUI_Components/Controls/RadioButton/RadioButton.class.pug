-

  class RadioButton__YDF {

    /**
     * @public
     * @readonly
     * @type string
     */
    static CSS_NAMESPACE = "RadioButton--YDF";


    /* ━━━ Theming ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ */

    /**
     * @public
     * @type { { [themeName: string]: string } }
     * @description If you want to define new themes, do it only via `defineThemes` static method.
     */
    static Themes = { regular: "REGULAR" };

    /**
     * @public
     * @param { Array<string> } themesNames
     * @return { typeof RadioButton__YDF }
     */
    static defineThemes(themesNames) {
      return ComponentsAuxiliaries.defineThemes(themesNames, RadioButton__YDF);
    }

    /**
     * @private
     * @type { boolean } [ areThemesCSS_ClassesCommon = ComponentsAuxiliaries.areComponentsThemesCommon ]
     * */
    static areThemesCSS_ClassesCommon = ComponentsAuxiliaries.areComponentsThemesCommon;

    /**
     * @public
     * @returns { typeof RadioButton__YDF }
     * @public
     * */
    static considerThemesAsCommon() {
      RadioButton__YDF.areThemesCSS_ClassesCommon = true;
      return RadioButton__YDF;
    }


    /* ─── Geometry ───────────────────────────────────────────────────────────────────────────────────────────────── */
    static GeometricVariations = {
      regular: "REGULAR",
      small: "SMALL",
      minimalistic: "MINIMALISTIC"
    }

    /**
     * @public
     * @param { Array<string> } geometricVariationsNames
     * @return { typeof RadioButton__YDF }
     */
    static defineGeometricVariations(geometricVariationsNames) {
      return ComponentsAuxiliaries.defineGeometricVariations(geometricVariationsNames, RadioButton__YDF);
    }

    /**
     * @public
     * @readonly
     * @type {
     *   Readonly<{
     *     pillShape: string;
     *     squareShape: string;
     *     singleLine: string;
     *     noLeftBorderAndRoundings: string;
     *     noRightBorderAndRoundings: string;
     *     noTopBorderAndRoundings: string;
     *     noBottomBorderAndRoundings: string;
     *     noRoundings: string;
     *   }>
     * }
     */
    static GeometricModifiers = {
      pillShape: "PILL_SHAPE",
      squareShape: "SQUARE_SHAPE",
      singleLine: "SINGLE_LINE",
      noLeftBorderAndRoundings: "NO_LEFT_BORDER_AND_ROUNDINGS",
      noRightBorderAndRoundings: "NO_RIGHT_BORDER_AND_ROUNDINGS",
      noRoundings: "NO_ROUNDINGS"
    };


    /* ─── Decoration ─────────────────────────────────────────────────────────────────────────────────────────────── */
    /**
     * @public
     * @type { { [decorativeVariationsName: string]: string } }
     * @description If you want to define new decorative variations, do it only via `defineDecorativeVariations` static method.
     */
    static DecorativeVariations = {
      regular: "REGULAR"
    };

    /**
     * @public
     * @param { Array<string> } decorativeVariationsNames
     * @return { typeof RadioButton__YDF }
     */
    static defineDecorativeVariations(decorativeVariationsNames) {
      return ComponentsAuxiliaries.defineDecorativeVariations(decorativeVariationsNames, RadioButton__YDF);
    }


    /* ━━━ Properties Specification ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ */

    /**
     * @public
     * @readonly
     * @type { RawObjectDataProcessor.PropertiesSpecification }
     * */
    static propertiesSpecification = {

      label: {
        type: String,
        required: false,
        minimalCharactersCount: 1
      },

      externalLabelHTML_ID: {
        type: String,
        required: false,
        minimalCharactersCount: 1
      },

      HTML_Name: {
        type: String,
        required: true,
        minimalCharactersCount: 1
      },

      HTML_Value: {
        type: String,
        required: false,
        minimalCharactersCount: 1
      },

      /** @description
       * 1 minimal character is definitely not enough for unique string, but in MVC mode it could be the variable. */
      instanceID_UniqueDynamicPart: {
        type: String,
        required: false,
        minimalCharactersCount: 1
      },

      inputElementHTML_ID: {
        type: String,
        required: false,
        minimalCharactersCount: 1
      },

      checked: {
        type: Boolean,
        defaultValue: false
      },

      disabled: {
        type: Boolean,
        defaultValue: false
      },

      readonly: {
        type: Boolean,
        defaultValue: false
      },

      theme: {
        type: String,
        defaultValue: RadioButton__YDF.Themes.regular,
        allowedAlternatives: Object.
            entries(RadioButton__YDF.Themes).
            map(([ key, value ]) => ({ key: `RadioButton__YDF.Themes.${ key }`, value }))
      },

      areThemesCSS_ClassesCommon: {
        type: Boolean,
        defaultValue: ComponentsAuxiliaries.areComponentsThemesCommon
      },

      geometricVariation: {
        type: String,
        defaultValue: RadioButton__YDF.GeometricVariations.regular,
        allowedAlternatives: Object.
            entries(RadioButton__YDF.GeometricVariations).
            map(([ key, value ]) => ({ key: `RadioButton__YDF.GeometricVariations.${ key }`, value }))
      },

      geometricModifiers: {
        type: Array,
        defaultValue: [],
        element: {
          type: String,
          allowedAlternatives: Object.values(RadioButton__YDF.GeometricModifiers)
        }
      },

      decorativeVariation: {
        type: String,
        defaultValue: RadioButton__YDF.DecorativeVariations.regular,
        allowedAlternatives: Object.
            entries(RadioButton__YDF.DecorativeVariations).
            map(([ key, value ]) => ({ key: `RadioButton__YDF.DecorativeVariations.${ key }`, value }))
      }

    }


    /* ━━━ ID generating ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ */
    /**
     * @public
     * @param { string } [uniqueDynamicPart = ComponentsAuxiliaries.generateUniqueDynamicPartForComponentInstanceID()]
     * @returns { string }
     */
    static generateInstanceID(
      uniqueDynamicPart = ComponentsAuxiliaries.generateUniqueDynamicPartForComponentInstanceID()
    ) {
      return `RADIO_BUTTON--YDF-${ uniqueDynamicPart }`;
    }

  }
