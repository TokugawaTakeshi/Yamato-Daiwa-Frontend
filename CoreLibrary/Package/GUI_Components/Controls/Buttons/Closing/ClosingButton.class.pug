include ClosingButtonLocalization.english.pug


-

  class ClosingButton__YDF {

    static CSS_NAMESPACE = "ClosingButton--YDF";

    static LabelLetterCases = {
      uppercase: "UPPERCASE",
      lowercase: "LOWERCASE",
      capitalisation: "CAPITALISATION"
    };


    /* ━━━ Theming ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ */
    static Themes = { regular: "REGULAR" };

    static defineThemes(themesNames) {
      return ComponentsAuxiliaries.defineThemes(themesNames, ClosingButton__YDF);
    }

    static areThemesCSS_ClassesCommon = ComponentsAuxiliaries.areComponentsThemesCommon;

    static considerThemesAsCommon() {
      ClosingButton__YDF.areThemesCSS_ClassesCommon = true;
      return ClosingButton__YDF;
    }


    /* ─── Geometry ───────────────────────────────────────────────────────────────────────────────────────────────── */
    static GeometricVariations = { regular: "REGULAR" };

    static defineGeometricVariations(geometricVariationsNames) {
      return ComponentsAuxiliaries.defineGeometricVariations(geometricVariationsNames, ClosingButton__YDF);
    }


    /* ─── Decoration ─────────────────────────────────────────────────────────────────────────────────────────────── */
    static DecorativeVariations = {
      regular: "REGULAR",
      lightForeground: "LIGHT_FOREGROUND"
    };

    static defineDecorativeVariations(decorativeVariationsNames) {
      return ComponentsAuxiliaries.defineDecorativeVariations(decorativeVariationsNames, ClosingButton__YDF);
    }


    /* ━━━ Properties specification ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ */
    static propertiesSpecification = {

      noLabel: {
        type: Boolean,
        undefinedValueSubstitution: false,
        isNullForbidden: true
      },

      labelLetterCase: {
        type: String,
        undefinedValueSubstitution: ClosingButton__YDF.LabelLetterCases.uppercase,
        isNullForbidden: true,
        allowedAlternatives: Object.values(ClosingButton__YDF.LabelLetterCases)
      },

      theme: {
        type: String,
        undefinedValueSubstitution: ClosingButton__YDF.Themes.regular,
        isNullForbidden: true,
        allowedAlternatives: Object.
            entries(ClosingButton__YDF.Themes).
            map(([ key, value ]) => ({ key: `ClosingButton__YDF.Themes.${ key }`, value }))
      },

      areThemesCSS_ClassesCommon: {
        type: Boolean,
        undefinedValueSubstitution: ClosingButton__YDF.areThemesCSS_ClassesCommon,
        isNullForbidden: true
      },

      geometricVariation: {
        type: String,
        undefinedValueSubstitution: ClosingButton__YDF.GeometricVariations.regular,
        isNullForbidden: true,
        allowedAlternatives: Object.
            entries(ClosingButton__YDF.GeometricVariations).
            map(([ key, value ]) => ({ key: `ClosingButton__YDF.GeometricVariations.${ key }`, value }))
      },

      decorativeVariation: {
        type: String,
        undefinedValueSubstitution: ClosingButton__YDF.DecorativeVariations.regular,
        isNullForbidden: true,
        allowedAlternatives: Object.
            entries(ClosingButton__YDF.DecorativeVariations).
            map(([ key, value ]) => ({ key: `ClosingButton__YDF.DecorativeVariations.${ key }`, value }))
      }

    };


    /* ━━━ Localization ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ */
    static localization = closingButtonLocalization__english;

  }
