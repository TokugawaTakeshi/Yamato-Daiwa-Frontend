include HamburgerMenuButtonLocalization.english.pug


-

  class HamburgerMenuButton__YDF {

    static CSS_NAMESPACE = "HamburgerMenu--YDF";

    static LabelLetterCases = {
      uppercase: "UPPERCASE",
      lowercase: "LOWERCASE",
      capitalisation: "CAPITALISATION"
    };


    /* ━━━ Theming ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ */
    static Themes = { regular: "REGULAR" };

    static defineThemes(themesNames) {
      return ComponentsAuxiliaries.defineThemes(themesNames, HamburgerMenuButton__YDF);
    }

    static areThemesCSS_ClassesCommon = ComponentsAuxiliaries.areComponentsThemesCommon;

    static considerThemesAsCommon() {
      HamburgerMenuButton__YDF.areThemesCSS_ClassesCommon = true;
      return HamburgerMenuButton__YDF;
    }


    /* ─── Geometry ───────────────────────────────────────────────────────────────────────────────────────────────── */
    static GeometricVariations = { regular: "REGULAR" };

    static defineGeometricVariations(geometricVariationsNames) {
      return ComponentsAuxiliaries.defineGeometricVariations(geometricVariationsNames, HamburgerMenuButton__YDF);
    }


    /* ─── Decoration ─────────────────────────────────────────────────────────────────────────────────────────────── */
    static DecorativeVariations = {
      regular: "REGULAR",
      lightForeground: "LIGHT_FOREGROUND"
    };

    static defineDecorativeVariations(decorativeVariationsNames) {
      return ComponentsAuxiliaries.defineDecorativeVariations(decorativeVariationsNames, HamburgerMenuButton__YDF);
    }


    /* ━━━ Properties specification ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ */
    static propertiesSpecification = {

      noLabel: {
        type: Boolean,
        undefinedValueSubstitution: false,
        isNullForbidden: true
      },

      alwaysEnglishLabel: {
        type: Boolean,
        undefinedValueSubstitution: false,
        isNullForbidden: true
      },

      animation: {
        type: Boolean,
        isUndefinedForbidden: false,
        isNullForbidden: true
      },

      labelLetterCase: {
        type: String,
        undefinedValueSubstitution: HamburgerMenuButton__YDF.LabelLetterCases.uppercase,
        isNullForbidden: true,
        allowedAlternatives: Object.values(HamburgerMenuButton__YDF.LabelLetterCases)
      },

      theme: {
        type: String,
        undefinedValueSubstitution: HamburgerMenuButton__YDF.Themes.regular,
        isNullForbidden: true,
        allowedAlternatives: Object.
            entries(HamburgerMenuButton__YDF.Themes).
            map(([ key, value ]) => ({ key: `HamburgerMenuButton__YDF.Themes.${ key }`, value }))
      },

      areThemesCSS_ClassesCommon: {
        type: Boolean,
        undefinedValueSubstitution: HamburgerMenuButton__YDF.areThemesCSS_ClassesCommon,
        isNullForbidden: true
      },

      geometricVariation: {
        type: String,
        undefinedValueSubstitution: HamburgerMenuButton__YDF.GeometricVariations.regular,
        isNullForbidden: true,
        allowedAlternatives: Object.
            entries(HamburgerMenuButton__YDF.GeometricVariations).
            map(([ key, value ]) => ({ key: `HamburgerMenuButton__YDF.GeometricVariations.${ key }`, value }))
      },

      decorativeVariation: {
        type: String,
        undefinedValueSubstitution: HamburgerMenuButton__YDF.DecorativeVariations.regular,
        isNullForbidden: true,
        allowedAlternatives: Object.
            entries(HamburgerMenuButton__YDF.DecorativeVariations).
            map(([ key, value ]) => ({ key: `HamburgerMenuButton__YDF.DecorativeVariations.${ key }`, value }))
      }

    };


    /* ━━━ Localization ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ */
    static localization = hamburgerMenuButtonLocalization__english;

  }
