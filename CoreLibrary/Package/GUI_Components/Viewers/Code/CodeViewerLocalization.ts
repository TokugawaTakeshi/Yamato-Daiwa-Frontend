export type CodeViewerLocalization = Readonly<{
  actionBar: Readonly<{
    codeCopyingButton: Readonly<{ label: string; }>;
  }>;

  tabsFlow: Readonly<{
    accessibilityGuidance: string;
  }>;

  tab: Readonly<{ languageKeyLabel: string; }>;

}>;
