-

  const ReusableTextings = {

    GUI_Components: {

      commonExampleWebViewer: {

        generateDescription: ({ componentName }) =>
          `Примеры использования компонента графического пользовательского интерфейса «${ componentName }» библиотеки ` +
            `«Yamato Daiwa Frontend» (сокращённо: «YDF»). `

      }

    }

  };
